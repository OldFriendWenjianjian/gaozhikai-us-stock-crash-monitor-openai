<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="高志凯美股崩盘概率"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".RiskWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.codexrisk.widget.REFRESH" />
                <action android:name="com.codexrisk.widget.PRE_TARGET_REFRESH" />
                <action android:name="com.codexrisk.widget.VISIBLE_EDGE_REFRESH" />
                <action android:name="com.codexrisk.widget.DAILY_REFRESH" />
                <action android:name="com.codexrisk.widget.FALLBACK_REFRESH" />
                <action android:name="com.codexrisk.widget.RETRY_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/risk_widget_info" />
        </receiver>

        <receiver
            android:name=".RiskUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.codexrisk.widget.PRE_TARGET_REFRESH" />
                <action android:name="com.codexrisk.widget.VISIBLE_EDGE_REFRESH" />
                <action android:name="com.codexrisk.widget.DAILY_REFRESH" />
                <action android:name="com.codexrisk.widget.FALLBACK_REFRESH" />
                <action android:name="com.codexrisk.widget.RETRY_REFRESH" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".RiskRefreshJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
</manifest>
